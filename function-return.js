function tenTimes(number) {
    const result = number * 10;
    return result;
}

function cutHalf(num) {
    const half = num / 2;
    return half;
}

tenTimes(5);

const output = tenTimes(7);
console.log('output', output);

const ordhek = cutHalf(100);
console.log('ordhek', ordhek);